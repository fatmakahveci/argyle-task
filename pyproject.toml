[tool.poetry]
name = "argyle-task"
version = "0.1.0"
description = ""
authors = ["fatmakhv <fatmaba@gmail.com>"]
readme = "README.md"
packages = [{include = "argyle_task", from = "src"}]

[tool.poetry.dependencies]
python = "^3.11"
httpx = "^0.28.1"
pydantic = "^2.12.5"


[tool.poetry.group.dev.dependencies]
pytest = "^9.0.3"
pytest-asyncio = "^1.3.0"
respx = "^0.22.0"

[tool.pytest.ini_options]
asyncio_mode = "auto"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
